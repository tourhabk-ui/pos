/**
 * Раскладка экрана /planner относительно шапки сайта.
 *
 * Шапка (`components/layout/Header.tsx`) — `position: fixed; top: 0` на
 * каждом экране. Страницы, построенные на `.ds-page`, получают отступ под
 * неё из globals.css. Планировщик — нет: он занимает весь экран колонками
 * (карта и план), и до 26.09 его корень стоял с верхнего края окна. На
 * телефоне полоса «План / Карта» ложилась поверх иконок шапки — поиска,
 * метки, темы, SOS, профиля и меню (снимок владельца, 390px, тёмная тема).
 *
 * Отступ берётся ТОТ ЖЕ, что у `.ds-page`: шапка одна, и свой второй
 * номер высоты разошёлся бы с ней при первой правке. Совпадение держит
 * сторож tests/unit/planner-steps.test.ts. Шапку ради этого не трогаем:
 * она общая, и остальные экраны от неё зависят.
 */
export const PLANNER_HEADER_OFFSET = 'calc(env(safe-area-inset-top, 0px) + 64px)';

/**
 * Запас снизу у содержимого шагов и результата: полоса шагов (её высоту
 * публикует сама полоса в `--page-action-bar-h`) плюс кнопка «Подобрать
 * тур», которая стоит над полосой (~56px и отступ), плюс воздух. Прежние
 * 112px (pb-28) не покрывали обе: последнее поле шага — морская болезнь,
 * номера рейсов — оставалось под кнопкой даже в конце прокрутки.
 */
export const CONTENT_BOTTOM_CLEARANCE = 'calc(var(--page-action-bar-h, 0px) + 112px)';
