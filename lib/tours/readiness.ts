/**
 * Готовность тура к чужой витрине — ОДНО правило на всех.
 *
 * Правило жило внутри крона переписи, а лента для Авито про него не знала:
 * она отдавала любой тур с `is_active AND is_published`, без описания, без
 * фото и без ответа «как турист попадает на тур». То есть перепись считала
 * готовность, а канал, ради которого она писалась, публиковал что попало —
 * и первым, кто узнал бы правду, был покупатель на чужой площадке под именем
 * нашего оператора.
 *
 * Поэтому правило переехало сюда, а перепись и каналы им ПОЛЬЗУЮТСЯ. Правило,
 * реализованное в двух местах, — это два правила, и они разойдутся (§12).
 */

export const MIN_DESCRIPTION_CHARS = 300;

export interface ReadinessRow {
  id: number;
  title: string;
  operator_id: string | null;
  operator_name: string | null;
  description_chars: number;
  photo_count: number;
  base_price: number | null;
  duration_hours: number | null;
  /**
   * Как турист попадает на тур (миграция 932). NULL — не записано.
   * `has_meeting_point` оставлен рядом: он отвечает на ДРУГОЙ вопрос («есть ли
   * текст точки сбора») и нужен, чтобы отличить перенесённые данные от новых.
   */
  pickup_type: 'hotel_pickup' | 'meeting_point' | 'self_drive' | null;
  pickup_details_chars: number;
  has_meeting_point: boolean;
  /** Условия отмены записаны оператором (колонка с миграции 931). */
  has_cancellation_policy: boolean;
  has_coords: boolean;
  has_operator_contact: boolean;
  included_count: number;
  program_steps: number;
}

/**
 * Чего не хватает конкретному туру. Пустой список — тур годен.
 *
 * ПРО `pickup` — поправка владельца 23.08. Первая версия называла это
 * «meeting_point» и считала пустое поле забывчивостью оператора. Это неверно:
 * операторы ЗАБИРАЮТ туристов сами, фиксированной точки сбора у таких туров
 * нет и быть не должно. Поэтому пустое поле здесь означает не «оператор не
 * заполнил», а «у нас не записано, КАК турист попадает на тур».
 *
 * Блокировать это всё равно приходится: чужая витрина обязана сказать
 * покупателю, ждать ли его у отеля или ехать самому, и «не знаю» тут не
 * публикуется. Но чинится оно не восемью письмами про точки сбора, а одной
 * фразой на оператора — где и в каких границах он забирает.
 */
export function missingFields(r: ReadinessRow): string[] {
  const missing: string[] = [];
  if (!r.title.trim()) missing.push('title');
  if (r.description_chars < MIN_DESCRIPTION_CHARS) missing.push('description');
  if (r.photo_count < 1) missing.push('photos');
  if (r.base_price === null || r.base_price <= 0) missing.push('base_price');
  if (r.duration_hours === null) missing.push('duration_hours');
  // Судим по pickup_type, а не по тексту точки сбора (932). Прежняя проверка
  // требовала meeting_point и тем самым требовала от оператора выдумать точку
  // сбора там, где он забирает туриста от отеля: поле отвечало не на тот
  // вопрос, и «пробел» у половины туров был пробелом нашей схемы, не данных.
  if (r.pickup_type === null) {
    missing.push('pickup');
  } else if (r.pickup_type !== 'self_drive' && r.pickup_details_chars === 0) {
    // «Заберут» и «встречаемся» без подробностей покупателю бесполезны: он не
    // знает ни границ забора, ни адреса. «Добирается сам» — исключение: куда
    // ехать, отвечают координаты тура, они проверяются отдельной строкой.
    missing.push('pickup_details');
  }
  // Условия отмены: покупатель на чужой витрине обязан знать, что будет с
  // деньгами при отмене, и «не знаю» тут не публикуется. До 931 это был
  // пробел схемы; теперь — пробел данных, чинится оператором в кабинете.
  if (!r.has_cancellation_policy) missing.push('cancellation_policy');
  if (!r.has_coords) missing.push('coordinates');
  if (!r.has_operator_contact) missing.push('operator_contact');
  return missing;
}

/**
 * Поля, которых нет НИ У ОДНОГО тура, потому что под них нет колонок.
 * Это факт о схеме, а не о данных: их нельзя «заполнить», их надо заводить.
 */
export const SCHEMA_GAPS = [
  { field: 'language',            note: 'язык проведения тура — колонки нет; для иностранной витрины это обычно обязательное поле' },
  // 'cancellation_policy' снят 03.09: колонка заведена миграцией 931, поле
  // переехало из пробелов схемы в пробелы данных (missingFields выше).
  { field: 'instant_confirmation',note: 'подтверждается ли бронь мгновенно — колонки нет; у нас подтверждение делает оператор' },
] as const;

/**
 * Блокер человеческими словами — для кабинета оператора и писем ему.
 *
 * Оператор — единственный, кто может это починить, и говорить с ним кодовым
 * именем поля бессмысленно. Формулировка отвечает на вопрос «что мне сделать»,
 * а не «какого поля нет в базе».
 */
export const BLOCKER_LABELS: Record<string, string> = {
  title: 'Нет названия тура',
  description: `Описание короче ${MIN_DESCRIPTION_CHARS} знаков — витрины столько требуют`,
  photos: 'Нет ни одной фотографии',
  base_price: 'Не указана цена',
  duration_hours: 'Не указана длительность',
  pickup: 'Не выбрано, как турист попадает на тур: заберёте, встретите или он едет сам',
  pickup_details: 'Не описано, откуда забираете или где встречаете',
  cancellation_policy: 'Не записаны условия отмены и возврата',
  coordinates: 'Нет координат на карте',
  operator_contact: 'В профиле компании нет контакта для связи',
};

/** Название блокера словами; неизвестный код показывается как есть. */
export function blockerLabel(field: string): string {
  return BLOCKER_LABELS[field] ?? field;
}
