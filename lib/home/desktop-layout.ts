/**
 * Одна сетка десктопной главной (решение владельца 25.09: «десктопная версия
 * просто говнище»).
 *
 * До этого у главной было ШЕСТЬ ширин: герой во весь экран, «Истории» у левого
 * края, туры в 1152, Кузьмич и настроения в 1120, «Стихии» и «Штурман» в 1488,
 * футер в 1104. У каждой секции был свой левый край — глаз читал это как
 * разваленную вёрстку раньше, чем успевал прочесть сами блоки.
 *
 * Теперь каждая секция десктопного дерева ставит контент в HOME_CONTAINER, а
 * вертикальный ритм — HOME_SECTION. Сторож: tests/unit/home-desktop-grid.test.ts.
 */
export const HOME_CONTAINER = 'mx-auto w-full max-w-[1280px] px-6 xl:px-8';

/** Вертикальный отступ секции — один на все, а не py-10/py-20/py-32 вразнобой. */
export const HOME_SECTION = 'py-12';
