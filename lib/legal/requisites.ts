/**
 * Кто мы по документам — в одном месте.
 *
 * До 11.09 наименование, ИНН, ОГРН и адрес стояли ДЕВЯТЬЮ вхождениями в пяти
 * юридических страницах плюс в шаблоне служебного письма. Сегодня они
 * совпадают — но совпадают по удаче, а не по устройству: сменится адрес или
 * директор, и часть страниц останется со старым.
 *
 * Цена расхождения здесь не косметическая. Реквизиты в оферте и политике —
 * это то, КОМУ человек платит и К КОМУ идёт с претензией; страница, назвавшая
 * их неверно, вводит потребителя в заблуждение (ЗоЗПП ст. 10 — обязанность
 * своевременно предоставлять достоверную информацию об исполнителе). Поэтому
 * источник один, а сторож `legal-requisites.test.ts` не даёт странице снова
 * написать их руками.
 *
 * ── Что здесь НЕ утверждается ─────────────────────────────────────────────
 *
 * Эти значения взяты из существующих страниц репозитория, а не из ЕГРЮЛ: я их
 * перенёс, а не проверил. Сверка с выпиской — за владельцем, и до неё
 * правильное слово «как записано у нас», а не «как есть на самом деле».
 */

export interface LegalRequisites {
  /** Полное наименование, как в ЕГРЮЛ. */
  fullName: string;
  /** Короткое, для текста. */
  shortName: string;
  /** Торговая марка — не юрлицо, и путать их нельзя. */
  brand: string;
  inn: string;
  ogrn: string;
  address: string;
  /** Единоличный исполнительный орган. */
  director: string;
  emailPrivacy: string;
  emailSupport: string;
  site: string;
}

export const REQUISITES: LegalRequisites = {
  fullName: 'ОБЩЕСТВО С ОГРАНИЧЕННОЙ ОТВЕТСТВЕННОСТЬЮ «ПОС-СЕРВИС»',
  shortName: 'ООО «ПОС-СЕРВИС»',
  brand: 'TourHab',
  inn: '4101147649',
  ogrn: '1114101005952',
  address: '683024, Камчатский край, г. Петропавловск-Камчатский, пр-кт 50 лет Октября, д. 17/1',
  director: 'Асеев Андрей Валерьевич',
  emailPrivacy: 'privacy@tourhab.ru',
  emailSupport: 'support@tourhab.ru',
  site: 'vedarai.ru',
};
