// Плоская конфигурация ESLint (flat config).
//
// Переезд с `.eslintrc.json` вынужденный, а не косметический: ESLint 9+ читает
// только flat config, а `eslint-config-next` 16 требует `eslint >= 9`. Оба
// мажора Dependabot (#825, #826) поэтому были одной задачей, а не двумя.
//
// Заодно `next lint` заменён на прямой вызов `eslint` в package.json: в
// Next 15.5 он объявлен устаревшим, а в Next 16 удалён вовсе — вызывать
// линтер напрямую значит не переписывать скрипт второй раз при подъёме Next.
//
// Набор правил ровно тот же, что был в `.eslintrc.json`: пресет
// `next/core-web-vitals` плюс шесть наших переопределений. Ничего не ужесточал
// и не ослаблял по дороге — иначе подъём версии превратился бы в тихую правку
// того, что репозиторий считает ошибкой.

import nextCoreWebVitals from 'eslint-config-next/core-web-vitals';

export default [
  // Пресет сам игнорирует .next/**, out/**, build/**, next-env.d.ts.
  ...nextCoreWebVitals,

  // Каталоги, до которых линтеру дела нет. В eslintrc их держал .eslintignore,
  // в flat config глобальные игноры — это запись с одним ключом `ignores`.
  {
    ignores: [
      'node_modules/**',
      '.next/**',
      'public/**',
      'coverage/**',
      'infra/ai-relay/node_modules/**',
      // Чужие собранные бандлы внутри скилла (modern-screenshot.umd.js и
      // соседи): 40 ошибок no-var/prefer-const в минифицированном коде, который
      // мы не писали и не правим.
      '.claude/skills/**/scripts/**',
      // Снятый с эксплуатации код: линтовать его — значит чинить то, что
      // не выполняется.
      '_archive/**',
    ],
  },

  {
    rules: {
      'prefer-const': 'error',
      'no-var': 'error',
      // no-console выключен намеренно: запрет `console.log` в проде живёт
      // отдельным детерминированным сторожем в тестах, а не правилом линтера,
      // который в CI не гоняется.
      'no-console': 'off',
      'no-debugger': 'error',
      'react/display-name': 'warn',
      '@next/next/no-img-element': 'warn',
      '@typescript-eslint/no-explicit-any': 'off',

      // ── Правила эры React Compiler, приехавшие с пресетом 16 ────────────
      //
      // В пресете 15.5 их не было вовсе; на нашем коде они дают 167 срабатываний
      // (133 из них — set-state-in-effect). Это не мусор: каждое указывает на
      // место, где эффект пишет состояние, мутируется то, что React считает
      // неизменяемым, или ref читается на рендере.
      //
      // Понижено до warn СОЗНАТЕЛЬНО и временно. Причины ровно две:
      //   1. Подъём версии не должен тащить за собой рефакторинг 167 мест в
      //      коде, часть которого — экраны безопасности. Это отдельная работа
      //      с отдельным риском регрессии, а не побочный эффект бампа.
      //   2. 'off' спрятал бы находки совсем, и мы бы решили, что чисто.
      //      warn держит их на виду в каждом прогоне.
      //
      // Возврат к 'error' — вместе с разбором списка, issue #885.
      // Не снимать правила, а чинить места.
      'react-hooks/set-state-in-effect': 'warn',
      'react-hooks/immutability': 'warn',
      'react-hooks/refs': 'warn',
      'react-hooks/purity': 'warn',
      'react-hooks/use-memo': 'warn',
    },
  },
];
