# Промпт для кодирующего агента: Редизайн [Название страницы/компонента]

**Контекст:**
Необходимо переработать [Платформа] версию [Название страницы] из "[Старая концепция]" в "[Новая концепция]". 

---

## Строгие правила дизайн-системы (из DESIGN.md):
- **Никакого Glassmorphism**: `backdrop-blur` и `bg-white/10` строго запрещены. Использовать только сплошные цвета `var(--bg-card)`.
- **Вложенность карточек**: Карточка внутри карточки запрещена.
- **Шрифты**: Заголовки — `Playfair Display` (`font-playfair`), текст — `Outfit`. Максимальная толщина — `font-bold` (без `font-black`).
- **Скругления**: Только `rounded-lg` (без `rounded-2xl`).
- **Цвета**: Только через CSS-переменные: `var(--bg-primary)`, `var(--bg-card)`, `var(--accent)`, `var(--ocean)`, `var(--success)`, `var(--danger)`. Никаких захардкоженных hex.
- **Иконки**: Только `lucide-react`. Никаких эмодзи в UI.

---

## Задача 1: [Название первого крупного изменения, например: Smart Header]
[Описание того, что нужно сделать].
- **Файл:** `[Путь к файлу или название нового файла]`.
- **Логика:** [Какие хуки, API или стейты использовать].
- **Визуал:** [Описание стилей, классов (например: ds-card, text-[var(--text-primary)])].

## Задача 2: [Название второго крупного изменения]
[Описание того, что нужно сделать].
- **Файл:** `[Путь к файлу]`.
- **Визуал:** 
  - [Деталь 1]
  - [Деталь 2]
  - [Деталь 3]

## Задача 3: [Название третьего крупного изменения, например: Bento Dashboard]
[Описание структуры и данных].
- **Файл:** `[Путь к файлу]`.
- **Данные:** [Откуда брать данные, API endpoints].
- **Структура сетки (CSS Grid):**
  - **Блок 1:** [Описание]
  - **Блок 2:** [Описание]

## Задача 4: Сборка и интеграция
- **Файл:** `[Главный файл, например: app/page.tsx]`
- **Логика:** [Как интегрировать новые компоненты с существующими, условия рендеринга, брейкпоинты].

```tsx
// Пример структуры (опционально)
<main>
  {/* Новый код */}
</main>
```
