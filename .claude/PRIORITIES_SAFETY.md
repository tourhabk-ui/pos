# Приоритеты — Безопасность и Юзабилити
> Записано: 6 мая 2026 | Следующая сессия: вечер 6 мая

---

## P0 — Жизнь и смерть (делать первым)

### 1. `/safety/offline` — страница выживания без интернета
- Статическая страница, без DB, без fetch
- Добавить в `PRECACHE_URLS` в `public/sw.js` → гарантированно доступна после установки PWA
- Бампнуть версию кеша (`CACHE_NAME = 'kamchatour-v7'`)
- **Содержание:**
  - Медведь — алгоритм действий (не бежать, говорить, медленно отступать)
  - Вулкан — признаки опасности (запах серы, тремор, пепел)
  - Гипотермия — признаки + первая помощь
  - Потерялся — стоп, осмотреться, не паниковать, ждать
  - Сигнализация спасателям (3 сигнала, зеркало, костёр)
  - Экстренные контакты: 112, МЧС Камчатки +7 (4152) 41-27-70
  - Как правильно назвать координаты спасателям

### 2. `/sos` в PRECACHE_URLS
- Проверить что `/sos` есть в `PRECACHE_URLS` в `public/sw.js`
- Если нет — добавить
- Страница уже работает (200, есть 112 + МЧС контакты)

---

## P1 — Безопасность маршрута (после P0)

### 3. МЧС-регистрация на странице маршрута `/routes/[id]`
- 154 маршрута с `mchs_registration_required = true` в БД
- Показать блок: телефон из `mchs_phone` + ссылка `forms.mchs.gov.ru/registration_tourist_groups/form`
- Добавить в `_RouteDetailClient.tsx`

### 4. Профили безопасности на карточке места `/places/[id]`
- 557/572 мест имеют `location_safety_profile` в БД (97%)
- Показать: опасности (`hazard_types`), высота (`altitude_m`), до медпомощи (`nearest_medical_km`)
- Поле `sat_communicator_required` → бейдж "нужен спутниковый коммуникатор"

---

## P2 — Базовая юзабилити (после P1)

### 5. GPS-треки на карте маршрута
- 51 маршрут с `geometry` (GeoJSON LineString)
- Отображать линию на Leaflet, кнопка "Скачать GPX"

### 6. Связь мест с маршрутами
- 311 мест без route_waypoints — агенты заполняют автоматически
- Не требует ручной работы

---

## Контекст сессии (что сделано сегодня)
- ✅ Race condition в бронированиях — исправлен (`app/api/hub/bookings/create/route.ts`)
- ✅ Страница `/partners` с аффилиатами — задеплоена
- ✅ 7 агентов отключены (расписание убрано, workflow_dispatch оставлен)
- ✅ 2 мёртвых эндпоинта удалены (run-089, run-115)
- ✅ Миграция 661 — индексы на `operator_bookings(created_at)` и `agent_memory(created_at)`
- ✅ Ссылка "Планирование поездки" добавлена в футер

## Технология офлайн (решено)
- Архитектура: статическая страница + `PRECACHE_URLS` в SW
- НЕ использовать: PDF, IndexedDB, Workbox — избыточно
- SW уже кеширует: последние 30 карточек мест + 525 тайлов карты Камчатки
