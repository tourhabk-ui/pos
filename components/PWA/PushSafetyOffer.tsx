'use client';

/**
 * Предложение подписаться на предупреждения о безопасности.
 *
 * ПОВОД (23.08.2026). Watchdog: «Push-канал пуст: подписчиков 0, доставлять
 * некому. 18 предупреждений не ушло — это следствие пустого канала, а не отказ
 * доставки». Механика при этом целая: service worker, VAPID, подписка,
 * отправка — всё на месте и работает. Не работало РАСПОЛОЖЕНИЕ: предложение
 * стояло на /safety и в личном кабинете, то есть там, куда человек заходит
 * УЖЕ решив позаботиться. Турист, который думает о безопасности, в этот момент
 * находится на подготовке к своему маршруту.
 *
 * Компонент один, а не по копии на экран. Текст обещания живёт здесь и только
 * здесь: две копии обещания — это два обещания, и однажды они разойдутся, как
 * разъехались двенадцать копий публичного URL.
 *
 * Само действие — PushSubscribeButton, второй реализации подписки нет. Кнопка
 * сама исчезает, когда предлагать нечего: push не поддержан, ключа нет, уже
 * подписан или заблокировано в браузере.
 */

import { PushSubscribeButton } from '@/components/PWA/PushSubscribeButton';

export function PushSafetyOffer({ className }: { className?: string }) {
  return (
    <div
      className={`ds-card ${className ?? ''}`}
      style={{
        padding: '14px 16px',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 12,
        flexWrap: 'wrap',
      }}
    >
      <div style={{ minWidth: 0 }}>
        <p style={{ fontWeight: 600, color: 'var(--text-primary)', fontSize: 14 }}>
          Предупреждения о безопасности
        </p>
        {/*
          Перечислены КОНКРЕТНЫЕ поводы, а не «важные уведомления». Человек
          соглашается на то, что ему назвали; обещать «важное» и слать что
          придётся — способ получить отписку и заслуженно.
        */}
        <p style={{ color: 'var(--text-secondary)', fontSize: 12, marginTop: 2 }}>
          Цунами, сейсмо, вулканы, перекрытия дорог — сразу на телефон, даже когда сайт закрыт.
        </p>
      </div>
      <PushSubscribeButton />
    </div>
  );
}
