-- 934: ответ владельца о подвозе — операторы забирают туристов сами.
--
-- 932 завела pickup_type с NULL как честным «у нас не записано»: у всех
-- восьми живых туров он и остался NULL, и перепись готовности держала
-- `pickup` первым блокером выкладки на Авито и Яндекс. Вопрос владельцу
-- 05.09 («одна фраза на оператора: забирает ли он туристов сам») получил
-- ответ: «забирает он туристов сам». Тот же ответ владелец давал 23.08
-- («операторы ЗАБИРАЮТ туристов сами, фиксированной точки сбора нет и быть
-- не должно») — то есть это правило витрины, а не свойство одного тура.
--
-- Заполняется ТОЛЬКО пустое: тур, у которого оператор уже выбрал тип в
-- кабинете, не перезаписывается. Подробности (откуда именно забирают) не
-- сочиняются — pickup_details остаётся NULL, пока оператор не напишет;
-- граница забора — не то, что можно вывести из слов владельца.
-- Только живые туры: у снятых с витрины ответ владельца не проверялся.

UPDATE operator_tours
   SET pickup_type = 'hotel_pickup'
 WHERE pickup_type IS NULL
   AND deleted_at IS NULL
   AND is_active = TRUE;

INSERT INTO _migrations (name)
VALUES ('934_operator_tours_pickup_owner_answer.sql')
ON CONFLICT (name) DO NOTHING;
