-- Migration 825: снять с повтора миграции, которые не пройдут или уже не нужны
--
-- Watchdog 05.08 наконец назвал причины поимённо — и они разные:
--
--   796  column "included" is of type jsonb but expression is of type text[]   (18 попыток)
--   800  null value in column "category" of relation "partners"                (15 попыток)
--   806  null value in column "category" of relation "partners"                (12 попыток)
--   819  COALESCE types jsonb and text[] cannot be matched                     (3 попытки)
--
-- Раннер повторяет непринятые миграции КАЖДЫЙ деплой. Сорок пять неудачных
-- попыток — это не безобидный шум: он держит в алерте четыре строки, среди
-- которых теряется настоящая авария. Сторож, который всегда красный, перестают
-- читать.
--
-- Разбираем по одной.
--
-- 800 и 806 делают `INSERT INTO partners`, не указывая `category`, а колонка
-- на проде NOT NULL без значения по умолчанию. Это не поправимо повтором: они
-- будут падать вечно. Их СОДЕРЖАНИЕ при этом давно доставлено другим путём —
-- 810 перепривязала тур к настоящему партнёру и вернула его в витрину без
-- единого INSERT, а 822 поставила верное имя и телефон. Повторять нечего.
--
-- 819 убирала аренду снастей и падала по типу колонок. Её работу делает 824,
-- уже после того как 823 привела тип. Повторять нечего.
--
-- 796 — особый случай, и поэтому она здесь. Её содержимое ХОРОШЕЕ: это
-- подтверждённый состав от оператора (трансфер, питание, гид и повар, удочки и
-- снасти, снаряжение), он заодно убрал бы с карточки «Трансфер из Елизово»,
-- которого никто не подтверждал. Но после 823 она применилась бы САМА на
-- следующем деплое и переписала бы состав целиком — молча и через шаг после
-- того, как о ней все забыли. Владелец 05.08 прямо просил обратного: сначала
-- показать, что в базе, и решать состав отдельно. Поэтому 796 снимается с
-- повтора, а её содержимое ждёт отдельной миграции по слову владельца — оно
-- никуда не делось, оно в git.
--
-- Запись в _migrations без выполнения — сознательный приём «superseded», а не
-- сокрытие. Причина каждой строки написана выше, файлы остаются в репозитории.
-- Идемпотентна.

INSERT INTO _migrations (name)
VALUES
  ('796_bystraya_tour_content_fields.sql'),
  ('800_bystraya_tour_force_publish.sql'),
  ('806_bystraya_tour_consolidate.sql'),
  ('819_bystraya_tackle_included.sql')
ON CONFLICT (name) DO NOTHING;

-- Запись о падении — теперь мусор: миграция снята с повтора осознанно, а не
-- «вот-вот починится». Пусть алерт показывает то, что действительно сломано.
DELETE FROM _migration_failures
 WHERE name IN (
   '796_bystraya_tour_content_fields.sql',
   '800_bystraya_tour_force_publish.sql',
   '806_bystraya_tour_consolidate.sql',
   '819_bystraya_tackle_included.sql'
 );
