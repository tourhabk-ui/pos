-- Migration 731: ЗАМЕНЕНА миграциями 736/737/738 — no-op.
--
-- Исходная версия падала на проде (inline-FK REFERENCES places(id) при
-- schema-drift, либо CREATE VIEW до добавления колонок) и никогда не
-- применялась. Правильный ремонт сделан атомарными 736 (duration_days),
-- 737 (merged_into_id без FK), 738 (VIEW). Эта миграция оставлена
-- безвредной заглушкой, чтобы не висеть в списке ошибок админ-раннера.

SELECT 1;
