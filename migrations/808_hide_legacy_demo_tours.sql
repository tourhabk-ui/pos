-- Migration 808: удалить старые демо-туры (не от наших партнёров)
--
-- Диагностика прода (03.08) показала: почти все туры приписаны к оператору
-- «Камчатская рыбалка», в т.ч. явно старые демо-попытки, к рыбалке не
-- относящиеся, с недействительными ценами и маркетинговым текстом. Партнёрский
-- фильтр (807) их НЕ ловит — они висят на реальном партнёре.
--
-- Решение владельца: эти демо-туры удалить. В платформе «удалить» тур = мягкое
-- удаление (deleted_at), как уже удалены зимние рыболовные туры 1,2,3,8 — строка
-- сохраняется ради целостности (брони operator_bookings, слоты tour_availability
-- не осиротеют), но из витрины и всех выборок (WHERE deleted_at IS NULL) тур
-- исчезает полностью. Хардовый DELETE НЕ используем — порвёт внешние ключи.
--
-- Остаются только актуальные туры двух партнёров: рыболовные Камчатской Рыбалки
-- (речной лосось) + сплав по Быстрой (Камчатка Рафтинг).
--
-- Матчим по НАЗВАНИЮ (устойчиво между окружениями; демо-название = тот же демо-
-- тур). Идемпотентна: трогает только ещё не удалённые.
--
-- «Морская рыбалка в Авачинской бухте» тоже сюда: это не речной продукт
-- «Камчатской Рыбалки», а демо (треска/палтус, катер, тот же рекламный стиль).

UPDATE operator_tours ot
   SET deleted_at   = NOW(),
       is_published = FALSE,
       is_active    = FALSE,
       updated_at   = NOW()
 WHERE ot.deleted_at IS NULL
   AND (
        ot.title ILIKE '%Авачинский вулкан%'
     OR ot.title ILIKE '%Вулкан Горелый%'
     OR ot.title ILIKE '%Медведи Курильского%'
     OR ot.title ILIKE '%Долина гейзеров%'
     OR ot.title ILIKE '%Термальные источники Паратунки%'
     OR ot.title ILIKE '%Дачные горячие источники%'
     OR ot.title ILIKE '%Морская рыбалка%'
     OR ot.title ILIKE '%хребет Вачкажец%'
   );
