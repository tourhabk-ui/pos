-- 895: у сложности появляется происхождение.
--
-- Бэкфилл сложности («го» владельца 21.08) считает её из набора высоты и
-- дистанции по шкале lib/routes/difficulty-scale. «Оператор сказал» и
-- «платформа посчитала» — разные состояния (правило третьего состояния),
-- и без следа они склеились бы навсегда: пересчитать шкалу v2 можно только
-- по своим записям, не трогая ручные. Образец — coord_source (миграция 873).
--
-- NULL — значение до-бэкфилльное (паспорт/ручное) или сложности нет вовсе.

ALTER TABLE kamchatka_routes ADD COLUMN IF NOT EXISTS difficulty_source VARCHAR(24);
