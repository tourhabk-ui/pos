{
  "note": "Маркер запуска cron-scout-digest.yml: правка этого файла в main прогоняет разведчика на проде (GET /api/cron/scout-digest) и печатает его ответ. Меняйте run, чтобы прогон был новым. Это ручной запуск, расписания у воркфлоу нет — суточный прогон идёт из оркестратора эволюции.",
  "run": 4,
  "run1_reason": "Проверка после выкатки 03.09: SCOUT_RELAY_BASE заведён на Timeweb; ждём в ответе via по лентам OpenAI/Google AI/DeepMind и блок ai_features линзы «ИИ-фичи для Ведара».",
  "run1_result": "curl exit 56 (соединение сброшено) на 16-й секунде — сетевой флап после выкатки a17b898: prod-check run 7 через три минуты дал 200 на шести адресах при аптайме процесса 874 с, то есть процесс не перезапускался.",
  "run2_reason": "Повтор после стабилизации: ждём via по лентам OpenAI/Google AI/DeepMind и блок ai_features.",
  "run2_result": "HTTP 200 за 77 с. Напрямую отдали материал 3 источника из 13 (Simon Willison, DeepMind, safety-слой), 10 упали по таймауту прямого запроса — сетевой флап прода в те минуты (среди упавших Habr и Турпром). Реле не вызывалось: в SCOUT_RELAY_BASE на Timeweb лежала строка с опечаткой, каждый фолбэк падал на разборе адреса (#1547 теперь называет это relay: bad_base). Линза: 6 кандидатов, 4 с текстом, модель вернула пусто (теперь код model_empty).",
  "run3_reason": "После деплоя #1547 и правки переменной владельцем: ждём relay: on, via по лентам OpenAI/Google AI, итог линзы.",
  "run3_result": "HTTP 200. Все 13 источников ok напрямую (via: direct) — реле в этом прогоне не понадобилось вовсе, поэтому relay: bad_base из ответа не блокирует ничего СЕГОДНЯ, но остаётся неснятым и вернётся, как только прямой путь снова упадёт. Настоящая причина немоты — не реле: digest_skip_reason unsupported_claims, судья не принял «опубликовано 3 сентября 2026» (даты не было в источнике). Тем же прогоном (после него) в main ушёл #1552: писателю запрещено называть даты публикации, которых нет дословно в сигнале.",
  "run4_reason": "Разбор задачи «Разведчик молчит с 01.08» (#68 бэклога): проверить, публикуется ли выпуск теперь, когда запрет на выдуманные даты (#1552) уже в main. relay: bad_base — отдельная, всё ещё открытая находка (переменная на Timeweb не решает разбор адреса), но не блокер для этого прогона."
}
