{
  "purpose": "Файл-маркер: правка этого файла в main запускает evo-review.yml немедленно (у агента в репозитории нет actions:write). Тот же приём, что evo-judge.json.",
  "last_trigger": "2026-08-27 — первый боевой прогон после починки YAML (python-heredoc рвал литеральный блок, workflow не парсился с момента создания)"
}
