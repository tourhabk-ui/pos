{
  "note": "Маркер запуска openrouter-models.yml: правка этого файла в main спрашивает каталог OpenRouter С РАННЕРА (прод его не видит — 403). Только чтение, ни одного запроса к модели. Поле match — регулярка по id и имени.",
  "run": 7,
  "match": "glm|z-ai|zhipu|claude-(opus|fable)",
  "run1_reason": "07.09, владелец: «в опенроутере попробуй gpt6 астра». Id модели выдумывать нельзя — неверный id даёт не «модель молчит», а тихий отказ на каждом вызове. Сначала спрашиваем каталог: есть ли такая модель, как она называется точно и почём.",
  "run2_reason": "09.09, владелец: «если это экономит деньги переключай на GLM флагман». Условие проверяемое, и проверять его надо каталогом, а не памятью о прайсе. Регулярка нарочно берёт ОБЕ стороны сравнения — все варианты GLM/z.ai и нынешний флагман Anthropic, — чтобы экономия была видна одной таблицей, а не выведена из двух разных источников. Заодно каталог называет точный слаг вендора: у z.ai он может быть z-ai, zhipu или иным, и угадывание тут стоит тихого отказа на каждом вызове.",
  "run3_reason": "18.09, отчёт судьи (#1428): `flagship(z-ai/glm-5.3): HTTP 401 {\"error\":{\"message\":\"Missing Authentication header\",\"code\":401}}`, четыре верхние ступени решателя молчат, вердикты выносит deepseek-flash — вопреки §8 («решатель дипсик либо опус»). Владелец показал счёт OpenRouter: $18.87 доступно, значит дело не в деньгах. Но каталог OpenRouter публичный и о ключе не говорит НИЧЕГО, поэтому прежние прогоны этой пробы годились для цен и не годятся здесь. Добавлен шаг /api/v1/key: требует авторизации, не стоит ни одного токена и разделяет две разные починки — «заменить секрет OPENROUTER_API_KEY» и «поправить интеграцию z.ai (BYOK) в кабинете». Тело ошибки — в форме самого z.ai, что склоняет ко второму, но склоняет не значит показывает.",
  "run3_result": "ОТВЕТ ЕСТЬ, и он снял мою версию про z.ai. /api/v1/key с раннера: HTTP 401, тело {\"error\":{\"message\":\"Missing Authentication header\",\"code\":401}} — то есть отвергнут САМ секрет OPENROUTER_API_KEY на эндпоинте авторизации OpenRouter, а не маршрут к z-ai/glm-5.3. Каталог и цены не спрашивались: шаг красный, дальше прогон не пошёл.",
  "run4_reason": "Различить две починки одного 401: ключ отозван или вставлен с лишним символом. OpenRouter отвечает «Missing Authentication header» в обоих случаях — заголовок с пробелом внутри для него отсутствует. Шаг при отказе печатает форму ключа без содержимого: длина, начинается ли на sk-or-, есть ли пробелы. Тот же приём, что у describeOpenRouterKey() для прода.",
  "run4_result": "ФОРМА КЛЮЧА: длина=108, начинается на sk-or-=НЕТ, пробелы внутри=нет. Ключи OpenRouter всегда начинаются на sk-or- и заметно короче — значит в секрете лежит НЕ ключ OpenRouter, а ключ другого поставщика (108 знаков — характерная длина ключа Anthropic sk-ant-api03-...). Это же объясняет «Missing Authentication header»: OpenRouter не разбирает чужую форму. Версия «ключ отозван» отпадает, версия «ключи разложены не по своим секретам» проверяется прогоном 5.",
  "run5_reason": "Назвать поставщика по началу ключа (sk-or- / sk-ant- / sk-proj- / иной) — одно слово наружу, ни одного символа ключа. Если ответ «ANTHROPIC» — чинится не выпуском новых ключей, а возвратом их по своим местам; тогда и «API key is invalid» у Anthropic объясняется тем же обменом.",
  "run5_result": "НАЗВАНО: «чей ключ по началу: ANTHROPIC (sk-ant-) — ключ лежит не в своём секрете», длина 108, пробелов нет. В секрете OPENROUTER_API_KEY лежит ключ Anthropic. Значит ни один ключ не отозван и новые выпускать не нужно: ключи разложены не по своим секретам, и починка — вернуть их на места. Отсюда же «Missing Authentication header» (OpenRouter не разбирает чужую форму) и, вероятно, «API key is invalid» у Anthropic (ему отдают не его ключ).",
  "run6_reason": "Проверка после возврата ключей на места. Прогон стоит ноль токенов и отвечает однозначно: 200 на /api/v1/key со метаданными — ключ OpenRouter лежит в своём секрете и принят; повторное «ANTHROPIC (sk-ant-)» — обмен ещё не сделан. Без этой проверки «я поменял» и «работает» остаются разными утверждениями, а различить их нечем.",
  "run6_result": "Красный, то же самое: 401, длина 108, ANTHROPIC (sk-ant-). Ожидаемо — человек секретов ещё не трогал. Прогон записан как «ещё нет», а не как новость.",
  "run7_reason": "Версия «ключи поменяли местами» ПОЛОВИНЧАТА, и это показала проба anthropic-path-probe (прогон 11, того же дня): в секрете ANTHROPIC_API_KEY лежит ключ Anthropic (sk-ant-, длина 108) — в своём секрете, — и Anthropic отвечает ему «API key is invalid». Значит ключей формы sk-ant- у нас ДВА, оба длиной 108, и по началу они неотличимы, а починки у них разные: если живой ключ лежит в секрете OpenRouter — его надо переложить и новый не выпускать; если оба мертвы — выпустить. Различить может только сам Anthropic, поэтому шаг спрашивает у него /v1/models тем ключом, что лежит в OPENROUTER_API_KEY: авторизация нужна, токены не тратятся, раннер не в РФ. Три исхода названы отдельно — 200 «переложить», отказ «нужен новый», сеть не дошла «не знаю». Сторож: tests/unit/key-family-probe.test.ts."
}
