{
  "reason": "08.09, прогон 5: владелец пополнил баланс на 29$ и просил попробовать Fable 5.1 вместо Astra. Тариф у неё тот же ($10/$50 за млн) — это проба КАЧЕСТВА, а не экономия. Первый прогон, где проход сохраняет находки в артефакт: результат переживёт нехватку денег на круге проверки, и добивать список можно будет verify-only без нового платного прохода",
  "model": "anthropic/claude-fable-5-1",
  "verify_model": "deepseek/deepseek-chat",
  "targets": [],
  "note": "model — основной проход. У Claude 4.6+ снято сэмплирование: temperature раннер таким моделям не шлёт (иначе 400), см. MODEL_PROFILES в scripts/os-audit-runner.ts. verify_model — круг проверки: задача узкая, флагман не нужен, а отдельный судья независимее того, кто находку написал. targets пуст — набор по умолчанию. Разобранное лежит в .github/audit-journal.json: аудит его читает и не разбирает второй раз, но и НЕ ГЛУШИТ по нему находки — совпавшие уходят в конец очереди проверки."
}
