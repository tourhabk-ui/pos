name: Cron Kuzmich Sezon

on:
  schedule:
    - cron: '23 16 * * 2,6'  # 16:23 UTC вт/сб — сезонный пост Кузьмича (нечётная минута: GitHub дропает запуски ровно в :00)
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Kuzmich seasonal post
        run: |
          curl -X GET "https://vedarai.ru/api/cron/kuzmich?type=sezon" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json" \
            -H "User-Agent: GitHub-Actions" \
            --max-time 120 \
            --fail
