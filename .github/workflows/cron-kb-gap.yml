name: Cron — KB Gap (пробелы в знаниях Кузьмича)

on:
  schedule:
    - cron: '0 4 * * *'  # 04:00 UTC ежедневно — анализ пробелов в знаниях
  workflow_dispatch:

jobs:
  kb-gap:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Analyze knowledge base gaps
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$CRON_SECRET" ]; then echo "CRON_SECRET not set"; exit 0; fi
          HTTP=$(curl -s -o /tmp/out.json -w "%{http_code}" --max-time 300 \
            -H "Authorization: Bearer $CRON_SECRET" \
            "https://vedarai.ru/api/cron/kb-gap")
          echo "HTTP: $HTTP" && cat /tmp/out.json 2>/dev/null || true
          [ "$HTTP" = "200" ] && echo "OK" || { echo "FAIL $HTTP"; exit 1; }
